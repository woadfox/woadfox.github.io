---
title: sample project
date: 2026-07-19
status: in progress
repo: https://github.com/woadfox/woadfox.github.io
---

A placeholder project entry so project formatting can be checked at every stage.
Projects can carry a `date`, a `status`, and a `repo` link in their front matter.

```python
def dye(fiber, vat="woad"):
    return f"{fiber} dyed with {vat}"
```

Delete this file once a real project exists.
